[project]
name = "vettr-scraper-sidecar"
version = "0.1.0"
description = "Scrapling-backed fetch/extract sidecar for the Vettr Source Recipe Platform"
requires-python = ">=3.12,<3.13"
dependencies = [
  "scrapling[fetchers]==0.4.15",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "pydantic>=2",
]

[tool.uv]
package = false
